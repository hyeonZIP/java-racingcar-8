# 자동차 경주 도메인 명세서

## 자동차(Car)

_Entity_

### 속성

- `name` : 자동차 이름
- `position` : 현재 위치

### 행위

- `static register()` : 자동차를 등록 한다 : name
- `increasePosition()` : 자동차의 위치를 증가 시킨다

### 규칙

#### 기본 규칙

- 자동차의 이름은 쉼표(,)를 기준으로 구분한다
- 자동차의 이름은 5자 이하만 가능하다
- 자동차의 초기 위치는 0 이다

#### 커스텀 규칙

- 자동차의 이름은 1자 이상 5자 이하만 가능하다

## 자동차 목록(Cars)

_First-Class Collections_

### 속성

- `cars` : Car 리스트
- `moveStrategy` 자동차 이동 전략 인터페이스

### 행위

- `getGameWinners()` : 가장 멀리간 자동차들을 반환한다
- `getRoundResult()` : 라운드 결과를 반환한다

### 규칙

#### 기본 규칙

- None

#### 커스텀 규칙

- 최소 1개 이상의 자동차가 등록되어야 한다
- 자동차 이름은 중복이 불가능하다

## 시도 횟수(Round)

_ValueObject_

### 속성

- `round` : 시도 횟수

### 행위

- `static of()` : 시도 횟수를 저장한다
- `static initialRound()` : 라운드를 초기화 한다
- `nextRound()` : 현재 라운드를 증가 시킨다
- `hasNextRound()` : 다음 라운드가 존재여부를 반환한다

### 규칙

#### 기본 규칙

- None

#### 커스텀 규칙

- 시도 횟수는 1~100 사이의 정수만 가능하다

## 경주 게임(RacingGame)

_Entity_

### 속성

- `cars` : 참여하는 자동차들
- `totalRound` : 시도 횟수
- `currentRound` : 현재 시도 횟수

### 행위

- `static initializeGame()` : 게임 초기화를 한다
- `playRound()` : 라운드를 시작한다

### 규칙

#### 기본 규칙

- None

#### 커스텀 규칙

- None

## 이동 전략(MoveStrategy)

_Interface_

### 행위

- `canMove()` : 자동차가 움직일 수 있는지를 반환한다

### 구현체

- `RandomMoveStrategy` : 랜덤 숫자를 생성하고 4이상일 시 이동한다
